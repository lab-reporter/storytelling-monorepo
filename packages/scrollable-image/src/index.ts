import { ScrollableImageEditor } from './cms-editor/index'
import { ScrollableImage } from './scrollable-image'
import { ScrollableImageForKeystoneEditorCMS } from './scrollable-image-for-keystone-editor'

export {
  ScrollableImageEditor,
  ScrollableImage,
  ScrollableImageForKeystoneEditorCMS,
}

export default {
  ScrollableImageEditor,
  ScrollableImage,
  ScrollableImageForKeystoneEditorCMS,
}
