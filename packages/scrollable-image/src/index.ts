import { ScrollableImageEditor } from './cms-editor/index'
import { ScrollableImage } from './scrollable-image'

export { ScrollableImageEditor, ScrollableImage }

export default {
  ScrollableImageEditor,
  ScrollableImage,
}
