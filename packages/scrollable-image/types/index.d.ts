import { ScrollableImage, ScrollableImageEditor } from '../src/index'
import { ScrollableImageProps } from '../src/scrollable-image'
import { ScrollableImageEditorProps } from '../src/cms-editor/index'

export {
  ScrollableImage,
  ScrollableImageProps,
  ScrollableImageEditor,
  ScrollableImageEditorProps,
}
